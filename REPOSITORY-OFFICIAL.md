# Repositorio oficial

Este workspace representa o repositorio oficial:

- `MMax-Company/Plataforma-Medica-Mdoctor`

O repositorio `MMax-Company/Mdoctor-Prescreve` e legado de producao enquanto Railway ainda estiver apontando para ele.

Antes de commit, push ou deploy, rode:

```bash
npm run pre-github-check
```

Antes de mudar source repo/root directory no Railway, consulte:

- `docs/TRANSICAO-RAILWAY-GITHUB.md`

Para rodar localmente pelo VS Code, consulte:

- `docs/VS-CODE-LOCAL-DEV.md`
